<form method="POST" action="{{ path('season_delete', {'id': season.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer la saison?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn btn-sm btn-outline-light my-3">Supprimer</button>
</form>
